/**
 * Shared auth session type.
 *
 * Import as: import type { AppSession } from '@/auth'
 * (re-exported from auth.ts to avoid circular dependency issues)
 */

export type { AppSession } from '@/auth';
