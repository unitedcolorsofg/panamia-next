#!/bin/sh

# Invoke directly rather than via npx: husky puts node_modules/.bin first on
# PATH, and the bundled Git for Windows ships no bash.exe, so npx's
# "#!/usr/bin/env bash" shebang cannot resolve. "npx --no" already meant
# local-only resolution, so this is equivalent.
commitlint --edit "$1"
