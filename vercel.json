{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "[[ \"$VERCEL_GIT_COMMIT_REF\" == dependabot/* ]] || [[ $(git rev-parse --verify HEAD^2 2>/dev/null) ]] || git diff --quiet HEAD^ HEAD -- . ':!docs' ':!*.md'"
}
