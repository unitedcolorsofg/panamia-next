// Stub for next/dist/compiled/@vercel/og
// This app does not use next/og. The real package pulls in resvg.wasm (1346 KiB),
// yoga.wasm (87 KiB), and index.edge.js (797 KiB) — ~2.2 MiB that would otherwise
// be bundled into the Cloudflare Worker script.
// Aliased in next.config.js webpack config (server builds only).
module.exports = {};
